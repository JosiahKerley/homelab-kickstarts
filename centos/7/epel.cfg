%pre
URL=http://download.fedoraproject.org/pub/epel/7/x86_64
LOCALURL=http://mirror.kerley.io/epel/7/x86_64
if ping -c 10 mirror.kerley.io && curl -skL ${LOCALURL}
then
  URL=${LOCALURL}
fi
echo "repo --name='epel' --baseurl=${URL}" > /epel.cfg
%end
%include /epel.cfg
